package frc.robot.commands.auto.commandgroups.sixball;

import edu.wpi.first.wpilibj2.command.SequentialCommandGroup;

/**
 * Scores 6 balls with our partner feeding on our right when we are in front of the initiation line 
 * then drives to the far trench to collect 2 power cells and score them
 * @author Madison J. 
 * @category AUTON 
 */
public class CenterGoalFront6BallFeedRightTrench2BallShoot extends SequentialCommandGroup {
  @SuppressWarnings({"PMD.UnusedPrivateField", "PMD.SingularField"})

 /**
  * Scores 6 balls with our partner feeding on our right when we are in front of the initiation line 
  * then drives to the far trench to collect 2 power cells and score them
  */
  public CenterGoalFront6BallFeedRightTrench2BallShoot() {
    addCommands(
      
    );
  
  }

 
}
